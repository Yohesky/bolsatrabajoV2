window.addEventListener('load', inicio);

function inicio(){
	let idpropuesta = $('#estadisticas').attr('idpropuesta');
	let idusuario = $('#estadisticas').attr('idusuario');

	estadisticas(idpropuesta, idusuario);
}

function estadisticas(idpropuesta, idusuario){
	$.ge
}